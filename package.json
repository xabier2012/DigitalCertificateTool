{
  "name": "certificate-manager-tool",
  "version": "1.0.0",
  "description": "Herramienta de gestión de certificados digitales - 100% offline",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "build": "npm run build -w apps/desktop",
    "package": "npm run build -w packages/shared && npm run build -w packages/core && npm run package -w apps/desktop",
    "test": "npm run test -w packages/core",
    "build:e2e": "npm run build -w packages/shared && npm run build -w packages/core && npm run build:main -w apps/desktop && npm run build:renderer -w apps/desktop",
    "test:e2e": "npm run build:e2e && npx playwright test",
    "test:e2e:ui": "npm run build:e2e && npx playwright test --ui",
    "test:e2e:debug": "npm run build:e2e && set PWDEBUG=1 && npx playwright test",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
